[project]
name = "nano-optimize-driver"
authors = ["Web3 Dev Team"]
description = "Decentralized milestone tracking and optimization platform"
telemetry = false
requirements = []
[contracts.milestone-optimizer]
path = "contracts/milestone-optimizer.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
